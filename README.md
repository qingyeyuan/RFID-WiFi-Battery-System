# RFID+WiFi智能电池管理系统

## 📋 系统功能概述

### 1. RFID身份识别
- 内置唯一RFID芯片标识电池身份，确保电池-设备-换电站的精准匹配
- 换电时快速校验，自动拦截错装、混装，保障换电安全与兼容性

### 2. WiFi数据上传
- 实时上传电池关键数据：SOC（剩余电量）、电压、温度、湿度、RFID身份等
- 采用长连接TCP通信，确保电池-换电站-云端数据稳定交互

### 3. 状态监测与预警
- 实时在线监测电池状态与环境数据
- 温湿度超阈值自动报警（温度>25°C或湿度≥90%），通过蜂鸣器提示

### 4. 本地可视化显示
- OLED显示屏（128x64）实时显示电池电压、电量、温湿度、WiFi状态和RFID信息
- 直观展示系统运行状态，方便现场查看

### 5. 便捷配置与管理
- 自动配网功能，支持WiFi扫描和快速配置
- 长按Boot按钮重置配置，方便设备维护

### 6. 全生命周期追溯
- 建立电池唯一档案，通过设备ID和RFID实现身份识别
- 记录电池使用数据，便于质量追溯与维护决策

## 设备使用说明

### 🚀 首次使用步骤

1. **硬件连接**：按照最下方的附件中的接线表连接所有硬件组件

    **电池正极 → 120kΩ 电阻 → GPIO 34 **  

    **电池负极 → 24kΩ 电阻→ ESP32 GND**  

2. **上传代码**：将代码上传到 ESP32 开发板

3. **配网模式**：
   - 设备首次启动时，会自动进入配网模式
   - 打开手机或电脑的 WiFi 设置，连接名为 "Device-Config" 的热点
   - 热点密码：12345678

4. **配置设备**：
   - 连接热点后，打开浏览器，会自动跳转到配置页面
   - 填写 WiFi 名称、密码、服务器 IP 和端口
   - 点击 "保存配置" 按钮

5. **设备重启**：
   - 配置保存后，设备会自动重启
   - 重启后，设备会连接到配置的 WiFi 网络
   - 设备会每 3 秒发送一次数据到指定的 TCP 服务器

### 🔄 重新配置方法

如果需要重新配置设备，长按 Boot 按钮约 3 秒，设备会：
- 发出两次长蜂鸣声
- 清除现有配置
- 自动重启并进入配网模式

### 📡 数据传输

#### TCP 连接
- 采用长连接方式，中途不断开
- 服务器地址和端口可在配置页面设置

#### JSON 数据格式
```json
{
  "number": "25824",      // 设备ID（可在配置页面设置）
  "voltage": "3.05",      // 电池电压（V）
  "soc": "95",            // 电池电量（%）
  "temp": "25.5",         // 温度（℃）
  "humidity": "45.0",     // 湿度（%）
  "rfid": "A1B2C3D4"      // RFID 卡片ID
}
```

### 🔧 故障排除

1. **无法进入配网模式**：
   - 检查 Boot 按钮是否正常
   - 尝试重新上传代码

2. **WiFi 连接失败**：
   - 检查 WiFi 名称和密码是否正确
   - 确保 WiFi 信号强度足够

3. **TCP 连接失败**：
   - 检查服务器 IP 和端口是否正确
   - 确保服务器正在运行并监听指定端口

4. **传感器数据异常**：
   - 检查传感器接线是否正确
   - 确保传感器供电正常

### 📝 注意事项

- 请勿将 MFRC522 模块直接连接到 5V 电源，否则会烧毁模块
- 确保所有组件的 GND 都正确连接
- 电池检测电路必须使用 120kΩ 和 24kΩ 的电阻分压
- 配置完成后，设备会自动保存配置到 EEPROM，重启后无需重新配置

### 📋 功能特性

- 自动配网功能，支持 WiFi 配置
- 长连接 TCP 数据传输
- 实时温湿度监测
- 温湿度超阈值警报（温度>25°C或湿度≥90%）
- 电池电压和电量监测
- RFID 卡片识别
- 蜂鸣器提示
- OLED 显示屏（128x64）实时显示数据
- 配置重置功能
- 自动数据发送（每 3 秒一次）
- WiFi 扫描功能，方便配置

### 📎 附件：硬件接线表

#### 1. MFRC522 刷卡模块（SPI 总线）

| MFRC522 引脚 | ESP32 引脚 | 说明 |
| --- | --- | --- |
| **3.3V** | **3.3V** | 供电（**切勿接 5V**，否则烧模块） |
| **GND** | **GND** | 共地 |
| **SCK** | **GPIO 18** | SPI 时钟 |
| **MISO** | **GPIO 19** | SPI 主入从出 |
| **MOSI** | **GPIO 23** | SPI 主出从入 |
| **SDA (SS)** | **GPIO 5** | 片选信号 |
| **RST** | **GPIO 27** | 复位引脚 |

#### 2. SHT30 / SHT31 温湿度传感器（独立 I2C_1 总线）

| SHT3X 引脚 | ESP32 引脚 | 说明 |
| --- | --- | --- |
| **VCC** (或 VIN) | **3.3V** | 供电 |
| **GND** | **GND** | 共地 |
| **SDA** | **GPIO 32** | 独立数据线 |
| **SCL** | **GPIO 33** | 独立时钟线 |

#### 3. 有源蜂鸣器 (Buzzer)

| 蜂鸣器 引脚 | ESP32 引脚 | 说明 |
| --- | --- | --- |
| **VCC** | **3.3V** 或 **5V** | 看你的蜂鸣器支持多少伏 |
| **GND** | **GND** | 共地 |
| **I/O** (信号) | **GPIO 26** | 触发引脚（低电平触发） |

#### 4. OLED 显示屏 (128x64, I2C 总线)

| OLED 引脚 | ESP32 引脚 | 说明 |
| --- | --- | --- |
| **VCC** | **3.3V** | 供电 |
| **GND** | **GND** | 共地 |
| **SDA** | **GPIO 32** | I2C 数据线（与 SHT3X 共用） |
| **SCL** | **GPIO 33** | I2C 时钟线（与 SHT3X 共用） |

#### 5. 待测电池检测 (分压网络)

| 连接路径 | 说明 |
| --- | --- |
| **电池正极 → 120kΩ 电阻 → GPIO 34 ** | 分压网络，用于测量电池电压 |
| **电池负极 → 24kΩ 电阻→ ESP32 GND** | **非常重要：必须与 ESP32 共地！** |